# Database Configuration
POSTGRES_DB=job_scheduler
POSTGRES_USER=job_scheduler_app
POSTGRES_PASSWORD=your_password_here
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Application Ports
BACKEND_PORT=8080
FRONTEND_PORT=5173

# Frontend Configuration (used by Vite at build time)
# Automatically defaults to http://localhost:${BACKEND_PORT} if not set
VITE_API_BASE_URL=http://localhost:8080
